<h1><%= @artist.name %></h1>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, artist_song_path(@artist, song) %> <%= link_to "Edit Song", edit_artist_song_path(song.artist, song)%></li>
  <% end %>
</ul>
